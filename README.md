# Character-Level-Language-Model
Comprehensive educational project implementing RNNs and LSTMs from scratch using NumPy, combined with character-level language modeling to generate sequences (e.g., dinosaur names). The project covers forward and backward propagation, gradient clipping, and sampling for text generation.
